version: "3.9"
services:
  idp-api:
    build: .
    image: idp-service:latest
    ports:
      - "8000:8000"
    environment:
      - SERVICE_ENV=prod
      - LOG_LEVEL=INFO
    volumes:
      - ./data:/app/data
